{
  "name": "zwantum-blog-monorepo",
  "private": true,
  "description": "ZwantumBlog — Universal Reusable Blog Module Monorepo",
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\""
  },
  "devDependencies": {
    "turbo": "^2.4.4",
    "typescript": "^5.7.3",
    "vitest": "^3.0.7",
    "tsup": "^8.4.0",
    "rimraf": "^6.0.1"
  },
  "packageManager": "pnpm@11.22.0"
}
